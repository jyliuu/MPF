[project]
dependencies = [
    "numpy",
    "matplotlib"
]

[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[tool.maturin]
python-source = "python"
module-name = "mpf_py._mpf_py"
features = ["use-rayon"]
